<div>
<% for entry in @entries %>
  <h2><%= entry.title %></h2>
    <p><%= entry.date %></p>
    <p><%= entry.contents %></p>
    <h2><%= link_to(entry.title, entry_path(entry)) %></h2>
    <%= link_to("New entry", new_entry_path) %>
<% end %>
</div>


